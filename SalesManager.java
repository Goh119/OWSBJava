package managementsystem;

public class SalesManager extends User {
    public SalesManager(String username, String password) { //String userId, 
        super(username, password, "Sales Manager"); //userId, 
    }
}
